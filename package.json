{
  "name": "design-render-service",
  "version": "2.0.0",
  "description": "Design AI Banner Render Service - Playwright",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "postinstall": "npx playwright install chromium --with-deps"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "playwright": "^1.51.0"
  }
}
